services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: bikes
      POSTGRES_PASSWORD: bikes1234
      POSTGRES_DB: bikes_project
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
